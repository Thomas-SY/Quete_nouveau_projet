{% extends 'base.html.twig' %}

{% block title %}Tag{% endblock %}

{% block body %}
    <h1>Tag</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tag.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ tag.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tag_index') }}"><i class="fas fa-undo-alt"></i></a>

    <a href="{{ path('tag_edit', {'id': tag.id}) }}"><i class="fas fa-edit"></i></a>

    {{ include('tag/_delete_form.html.twig') }}
{% endblock %}
